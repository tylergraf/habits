
<div class="container">
  <div class="task-wrapper">
    <header class="task-header">
      <h2><%= data.date %></h2>
    </header>
    <section class="task-body">
      <ul class="task-list" id="task-list">
        <% for(var i = 0; i < data.checkIn.length; i++){ %> 
        <li class="task-item"><%= data.checkIn[i].title %></li>
        <% } %>
      </ul>
    </section>
  </div>
</div>

